name: check

on:
  workflow_dispatch:
  pull_request:
    types: [opened, reopened]
    branches-ignore:
      - 'create-pull-request/patch'

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          BUMP="patch"